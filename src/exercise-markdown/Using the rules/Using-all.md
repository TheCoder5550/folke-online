---
name: Universal introduction and elimination
order: 10
---

# Universal introduction and elimination
Create two fresh variables, `x_0` and `y_0`, and state that `P(x_0, y_0)` holds using `∀E`. Then use `∀I` to conclude that `P(u, v)` holds for *all* `u` and `v`.

!proof[sequent=∀ x ∀ y P(x, y) |- ∀ u ∀ v P(u, v)][solution=Using-all.folke]